@page "/Usuario/Register/{data?}"
@*@model Prueba.Areas.Usuario.Pages.Account.RegisterModel*@
@model RegisterModel
@{
    ViewData["Title"] = "Register";
}

<h1>@ViewData["Title"]</h1>

<h1>@Model.Message</h1>

<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card text-center">
                <div class="card-header">
                    <h3>Registrar</h3>
                </div>
                <img src="~/images/icons/logo.png" class="mx-auto w-25" alt="Alternate Text" />
            </div>
        </div>
    </div>
</div>

